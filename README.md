# S3 Versions To Git History

This repo will take an s3 bucket and for each object in the bucket build a git history for that file per version.
The commit message cannot be much more than the date change.
